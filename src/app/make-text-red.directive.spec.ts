import { MakeTextRedDirective } from './make-text-red.directive';

describe('MakeTextRedDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeTextRedDirective();
    expect(directive).toBeTruthy();
  });
});
